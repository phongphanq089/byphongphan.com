---
title: Card
description: Displays a card with header, content, and footer sections for grouped information architecture.
component: true
---

<ComponentPreview name="card-demo" />

## Installation

<CodeTabs defaultValue="manual">
  <TabsList>
    <TabsTrigger value="cli">Command</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>

  <TabsContent value="cli">
    <CodeBlockCommand name="card" />
  </TabsContent>

  <TabsContent value="manual">
    <Steps className="mb-0 pt-2">
      <Step>Install the following dependencies:</Step>
      <CodeBlockCommand command="npm install clsx tailwind-merge" />
      <Step>Copy and paste the card component into your project.</Step>
      <ComponentSource name="card" title="components/ui/card.tsx" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</CodeTabs>

## Usage

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from "@/components/ui/card"

<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Card Content</p>
  </CardContent>
  <CardFooter>
    <p>Card Footer</p>
  </CardFooter>
</Card>
```

---

## API Reference

### Card

Root surface container with border, background, and rounded elevation tokens.

| Prop | Type | Default |
| :--- | :--- | :--- |
| `className?` | `string` | - |
| `children` | `React.ReactNode` | - |

### CardHeader

Header section containing title and descriptive summary.

| Prop | Type | Default |
| :--- | :--- | :--- |
| `className?` | `string` | - |
| `children` | `React.ReactNode` | - |

### CardContent

Main body slot for layout blocks and interactive elements.

| Prop | Type | Default |
| :--- | :--- | :--- |
| `className?` | `string` | - |
| `children` | `React.ReactNode` | - |