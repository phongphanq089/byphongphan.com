---
title: Badge
description: Displays a badge or a component that looks like a badge for status tags and categorical labels.
base: radix
component: true
links:
  doc: https://www.radix-ui.com/primitives/docs/components/slot
---

<ComponentPreview name="badge-demo" />

## Installation

<CodeTabs defaultValue="manual">
  <TabsList>
    <TabsTrigger value="cli">Command</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>

  <TabsContent value="cli">
    <CodeBlockCommand name="badge" />
  </TabsContent>

  <TabsContent value="manual">
    <Steps className="mb-0 pt-2">
      <Step>Install the following dependencies:</Step>
      <CodeBlockCommand command="npm install @radix-ui/react-slot class-variance-authority clsx tailwind-merge" />
      <Step>Copy and paste the badge component into your project.</Step>
      <ComponentSource name="badge" title="components/ui/badge.tsx" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</CodeTabs>

## Usage

```tsx
import { Badge } from "@/components/ui/badge"

<Badge variant="default">New</Badge>
<Badge variant="secondary">In Progress</Badge>
<Badge variant="outline">Docs</Badge>
```

---

## API Reference

### Badge

Compact pill indicator for counts, status alerts, and categories.

| Prop | Type | Default |
| :--- | :--- | :--- |
| `variant?` | `"default" \| "secondary" \| "destructive" \| "outline"` | `"default"` |
| `asChild?` | `boolean` | `false` |
| `className?` | `string` | - |